apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pod-failure-experiment
  namespace: prism
spec:
  action: pod-failure
  mode: one
  duration: "30s"
  selector:
    namespaces:
      - prism
    labelSelectors:
      app: service-a
  scheduler:
    cron: "@every 10m"
